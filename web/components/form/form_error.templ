package form

import (
	"github.com/alextilot/golang-htmx-chatapp/web/utils"
)

templ Error(fieldErrors []string) {
	if utils.FieldHasErrors(fieldErrors) {
		<p class="text-error text-sm mt-1">
			{ utils.JoinErrors(fieldErrors) }
		</p>
	}
}
