package form

import (
	"github.com/alextilot/golang-htmx-chatapp/web/utils"
)

templ Input(attrs templ.Attributes, fieldErrors []string) {
	{{ classes := utils.HasErrorClass("input input-bordered w-full", fieldErrors, "input-error") }}
	<div class="form-control">
		<input
			{ attrs... }
			class={ classes }
			autocapitalize="none"
			autocorrect="off"
		/>
		@Error(fieldErrors)
	</div>
}
